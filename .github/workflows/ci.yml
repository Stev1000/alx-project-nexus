name: Minimal Test

on:
  push:
    branches: [ "master" ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print hello
        run: echo "Hello GitHub Actions is working!"
